/*const mongoose = require("mongoose");

chromosome = new mongoose.Schema({
  USER_ID: { type: String },
  ID: {
    type: String,
    //required: true,
  },
  POS: {
    type: String,
    //required: true,
  },

  REF: {
    type: String,
    //required: true,
    //1:1
  },
  FORMAT: {
    type: String,
    //required: true,
    //0:1
  },
  RES: {
    type: String,
    //required: true,
  },
  TYPE: {
    type: String,
    //required: true,
  },
});

//chromosome = mongoose.model("Chromosome", chromosome);
module.exports = chromosome = mongoose.model("chromosome", chromosome);
*/
